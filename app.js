// Generated by CoffeeScript 1.7.1
(function() {
  var PSD, rectangle, rectangle2, rectangle3;

  PSD = Framer.Importer.load("imported/google-reel");

  common.storeOriginal(PSD);

  Layer.prototype.fadeOut = function(delay) {
    return this.animate({
      properties: {
        opacity: 0
      },
      curve: 'ease-in',
      delay: delay
    });
  };

  PSD.container.brightness = 0;

  PSD.container.animate({
    properties: {
      brightness: 100
    },
    curve: "linear",
    time: 0.5
  });

  rectangle = new Layer({
    width: 400,
    height: 400,
    backgroundColor: "#fff",
    shadowY: 2,
    shadowBlur: 5,
    borderRadius: "6px"
  });

  rectangle.shadowColor = "rgba(0, 0, 0, 0.2)";

  rectangle.superLayer = PSD.container;

  rectangle.center();

  rectangle.states.add({
    centerInvisible: {
      scale: 0.01
    },
    centerBig: {
      scale: 1
    },
    rotated: {
      rotation: 90,
      height: 800,
      y: -40
    }
  });

  rectangle.states.animationOptions = {
    curve: "spring(200, 20, 10)"
  };

  rectangle2 = new Layer({
    opacity: 0,
    width: 400,
    height: 400,
    backgroundColor: "#fff",
    shadowY: 2,
    shadowBlur: 5,
    borderRadius: "6px"
  });

  rectangle2.superLayer = PSD.container;

  rectangle2.center();

  rectangle3 = new Layer({
    opacity: 0,
    width: 400,
    height: 400,
    backgroundColor: "#fff",
    shadowY: 2,
    shadowBlur: 5,
    borderRadius: "6px"
  });

  rectangle3.superLayer = PSD.container;

  rectangle3.center();

  rectangle.states.switchInstant("centerInvisible");

  utils.delay(0.5, function() {
    return rectangle.states["switch"]("centerBig");
  });

  utils.delay(1, function() {
    return rectangle.states["switch"]("rotated");
  });

  Layer.prototype.move = function(x, y) {
    return this.animate({
      properties: {
        x: this.x + x,
        y: this.y + y
      },
      curve: "spring(200, 20, 10)"
    });
  };

  Layer.prototype.fadeOut = function(delay) {
    return this.animate({
      properties: {
        opacity: 0
      },
      curve: 'ease-in',
      delay: delay,
      time: 0.2
    });
  };

  Layer.prototype.fadeIn = function(delay) {
    return this.animate({
      properties: {
        opacity: 1
      },
      curve: 'ease-in',
      delay: delay,
      time: 0.2
    });
  };

  utils.delay(1.05, function() {
    rectangle.fadeOut();
    rectangle2.fadeIn();
    rectangle3.fadeIn();
    rectangle2.move(-250, 0);
    return rectangle3.move(250, 0);
  });

}).call(this);
